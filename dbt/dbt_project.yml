name: northstyle
version: 1.0.0
config-version: 2

profile: northstyle

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
  - "logs"

models:
  northstyle:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
